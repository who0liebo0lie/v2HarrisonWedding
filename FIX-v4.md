# Cinematic viewport fix

This build pins both the sound entry screen and the complete cinematic sequence to the visible browser viewport. It also resets the page to the top before the intro starts and after it finishes, preventing the animation from playing above the visible screen when the browser restores an old scroll position.
