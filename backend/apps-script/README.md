# Google Apps Script backend

Copy `Code.gs` into a standalone Apps Script project. Add the Script Properties listed in the main SETUP-GUIDE.md, run `setupWeddingBackend()`, then deploy as a web app.
