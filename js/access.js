const gate=document.getElementById('welcome-gate');const form=document.getElementById('welcome-form');const closeBtn=document.getElementById('welcome-close');const saved=JSON.parse(localStorage.getItem('harrisonGuest')||'null');if(saved&&gate)gate.hidden=true;function closeGate(){if(gate)gate.hidden=true}closeBtn?.addEventListener('click',closeGate);form?.addEventListener('submit',e=>{e.preventDefault();const data=Object.fromEntries(new FormData(form));if(!data.firstName.trim()||!data.lastName.trim())return;localStorage.setItem('harrisonGuest',JSON.stringify(data));closeGate();});