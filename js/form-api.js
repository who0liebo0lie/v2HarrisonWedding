window.submitWeddingForm=async function(kind,data,status){const endpoint=kind==='rsvp'?WEDDING_CONFIG.rsvpEndpoint:WEDDING_CONFIG.pickleballEndpoint;status.textContent='Sending…';try{if(endpoint){const response=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'text/plain;charset=utf-8'},body:JSON.stringify({kind,...data,submittedAt:new Date().toISOString()})});if(!response.ok)throw new Error('Unable to submit');status.textContent='Thank you! Your response was received.'}else{const key=`wedding-${kind}-drafts`;const saved=JSON.parse(localStorage.getItem(key)||'[]');saved.push({...data,submittedAt:new Date().toISOString()});localStorage.setItem(key,JSON.stringify(saved));status.textContent='Your response is saved on this device. Online collection will begin when the backend URL is added.'}}catch(e){status.textContent='We could not submit your response. Please try again.'}};