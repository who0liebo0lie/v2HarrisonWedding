document.addEventListener("DOMContentLoaded",async()=>{const target=document.querySelector("#rooms");if(!target)return;try{const res=await fetch("data/room-types.json");const rooms=await res.json();target.innerHTML=rooms.map(r=>`<article class="room-card"><img src="${r.image}" alt="${r.name} stateroom"><div class="room-card-body"><h3>${r.name}</h3><p>${r.description}</p><div class="price">${r.price||"TBD"}</div><small>per person*</small><p><a class="button outline" href="${r.royalCaribbeanUrl}" target="_blank" rel="noopener">View on Royal Caribbean ↗</a></p></div></article>`).join("");}catch(e){target.innerHTML='<p>Room information will be added before booking opens.</p>'}});