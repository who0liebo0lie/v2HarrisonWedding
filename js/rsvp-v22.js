const cards=document.querySelector('#guest-cards'),count=document.querySelector('#guest-count'),form=document.querySelector('#rsvp-form-v22');const events=[['welcome','🌺 Welcome Party'],['bingo','Ⓑ Bingo'],['pickleball','🏓 Pickleball'],['wedding','💍 Wedding']];function render(){const old=[...cards.querySelectorAll('.guest-card')].map(c=>Object.fromEntries(new FormData(c.querySelector('form')||document.createElement('form'))));cards.innerHTML='';for(let i=1;i<=+count.value;i++){const card=document.createElement('section');card.className='guest-card';card.innerHTML=`<h2><span class="guest-badge">GUEST ${i}</span> Guest ${i}</h2><div class="guest-names"><label>First name<input type="text" name="guest${i}First" required></label><label>Last name<input type="text" name="guest${i}Last" required></label></div><div class="event-title">Please RSVP to each event</div><div class="event-grid">${events.map(([id,label])=>`<div class="event-choice"><strong>${label}</strong><div class="yesno"><label><input type="radio" name="guest${i}_${id}" value="yes" required>Yes, I’m in!</label><label><input type="radio" name="guest${i}_${id}" value="no">No, can’t make it</label></div></div>`).join('')}</div><div class="bingo-clue" hidden><h3>Your Bingo clue</h3><p>Share one surprising or fun fact about yourself.</p><textarea name="guest${i}BingoClue" rows="3"></textarea></div>`;cards.append(card);card.querySelectorAll(`input[name="guest${i}_bingo"]`).forEach(r=>r.addEventListener('change',()=>{const clue=card.querySelector('.bingo-clue'),ta=clue.querySelector('textarea');clue.hidden=r.value!=='yes'||!r.checked;ta.required=!clue.hidden;}));}}count.addEventListener('change',render);render();form.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(form),guests=[];for(let i=1;i<=+count.value;i++)guests.push({firstName:fd.get(`guest${i}First`),lastName:fd.get(`guest${i}Last`),events:{welcome:fd.get(`guest${i}_welcome`),bingo:fd.get(`guest${i}_bingo`),pickleball:fd.get(`guest${i}_pickleball`),wedding:fd.get(`guest${i}_wedding`)},bingoClue:fd.get(`guest${i}BingoClue`)||''});const payload={type:'rsvp',inviteCode:new URLSearchParams(location.search).get('invite')||localStorage.getItem('inviteCode')||'',householdName:localStorage.getItem('weddingGuestName')||'',guests,generalNote:fd.get('generalNote')||'',submittedAt:new Date().toISOString()};localStorage.setItem('lastRSVP',JSON.stringify(payload));const endpoint=window.WEDDING_BACKEND_URL||'';if(endpoint){try{await fetch(endpoint,{method:'POST',mode:'no-cors',headers:{'Content-Type':'text/plain'},body:JSON.stringify(payload)})}catch{}}document.querySelector('#rsvp-success').hidden=false;document.querySelector('#rsvp-success').scrollIntoView({behavior:'smooth'});});