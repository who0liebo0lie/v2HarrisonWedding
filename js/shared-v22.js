const page=document.body.dataset.page;document.querySelectorAll('.site-nav a').forEach(a=>{if(a.dataset.page===page)a.setAttribute('aria-current','page')});const toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('.site-nav');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));nav.classList.toggle('open',!open)});const overlay=document.querySelector('#welcome-overlay');if(overlay){const form=document.querySelector('#welcome-form');document.querySelector('.welcome-close').addEventListener('click',()=>overlay.remove());form.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(form);localStorage.setItem('weddingGuestName',`${d.get('firstName')} ${d.get('lastName')}`.trim());overlay.remove()});if(new URLSearchParams(location.search).get('skip')==='1')overlay.remove()}