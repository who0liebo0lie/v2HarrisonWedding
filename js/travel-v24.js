(()=>{const panel=document.querySelector('#faq-answer');const text=panel.querySelector('p');const close=panel.querySelector('button');const buttons=[...document.querySelectorAll('.faq-overlay button')];function hide(){panel.hidden=true;buttons.forEach(b=>b.setAttribute('aria-expanded','false'));}buttons.forEach((button,i)=>{button.type='button';button.setAttribute('aria-expanded','false');button.setAttribute('aria-controls','faq-answer');button.setAttribute('aria-label',`Open booking FAQ ${i+1}`);button.addEventListener('click',event=>{event.stopPropagation();const wasOpen=!panel.hidden&&button.getAttribute('aria-expanded')==='true';hide();if(!wasOpen){text.textContent=button.dataset.answer;panel.hidden=false;button.setAttribute('aria-expanded','true');}});});close.addEventListener('click',hide);panel.addEventListener('click',e=>e.stopPropagation());document.addEventListener('click',event=>{if(!event.target.closest('.faq-overlay')&&!event.target.closest('#faq-answer'))hide();});document.addEventListener('keydown',event=>{if(event.key==='Escape')hide();});})();